name: weave_flutter
description: Flutter integration for Weave AI framework
version: 0.1.0
repository: https://github.com/kitium-ai/weave

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  dio: ^5.0.0
  provider: ^6.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0
